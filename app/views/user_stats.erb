<h3>Way to go: <%= session[:user_email] %>!</h3>

<p>Here are your latest games: </p>

<% user_rounds.each do |round| %>

  <% true_guesses = Attempt.where(outcome: true).find_all_by_round_id(round).count %>
  <% total_guesses = Attempt.find_all_by_round_id(round).count %>

  <%= User.where(id: session[:id]).pluck(:email) %> got <%= true_guesses %> right out of <%= total_guesses %> for <%= (true_guesses.to_f/total_guesses) %>%. 
  <br>
<% end %>

<form class="logout" action="/sign_out" align="right" method="get">
  <input type="submit" value="Log Out">
</form>

<form class="home" action="/" align="right" method="get">
  <input type="submit" value="Home">
</form>
